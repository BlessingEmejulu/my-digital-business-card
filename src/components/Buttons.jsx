import React from "react";

export default function Buttons() {
    return (
        <div className="contact-btns">
            <a href="mailto:" className="contact-btn email-btn">Email</a>
            <a href="http://" className="contact-btn linkedIn-btn">LinkedIn</a>
        </div>

    );
}